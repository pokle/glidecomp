> Archived round of the [GlideComp security review log](../../security-review.md).
> Current finding statuses live in the index's register, not here.

# 2026-09-16 — Re-review (routine dependency upgrade window only; SEC-50 confirmed held; no new findings)

> **2 commits** since the prior round's reviewed HEAD (base `345fa81` → HEAD
> `06f7108`): the prior round's own security PR landing (#695, already fully
> accounted for in the [2026-09-09 round](2026-09-09.md) — its SEC-49 fix and
> SEC-50 dependency-override bumps are re-verified below, not re-derived),
> and the routine weekly `upgrade-deps` cycle (#696): ten package bumps (all
> minor/patch, no majors), one source change (`map-annotations.ts`, a
> mapbox-gl 3.30.0 API-compatibility fix), and the removal of a stray root
> `hono` dependency entry that #695 had left behind. **No new findings.**

### Methodology

- **Review base.** `345fa81` confirmed an ancestor of `HEAD` (`06f7108`);
  window `345fa81..06f7108`, 2 commits. The local branch
  (`claude/sharp-davinci-qh1dc9`) and `origin/master` are the same commit
  (`06f7108`) — a fresh branch off current `master`.
- **Infra diff** (`**/wrangler.toml`, `functions/**`, `**/migrations/**`,
  `.github/workflows/**`): zero diff across the whole window, checked both
  with a pathspec'd `git diff --stat` and (per the standing caveat about
  `.github/workflows/` sometimes being absent from a sandboxed checkout) by
  confirming the full `git diff --stat` (no pathspec) shows no path under
  `.github/` at all — nothing to fall back to the blob-diff trick for this
  round. `.github/workflows/` and `functions/` were present on disk this
  session.
- **Worker/engine diff** (`web/workers/**/src/**`, `web/engine/src/**`): only
  `web/workers/auth-api/src/index.ts` (+23/−3) and the new
  `web/workers/shared/src/name-text.ts` (+38) — both are #695's SEC-49 fix,
  already reviewed and fixed in the prior round; re-read here to confirm
  they landed unchanged (they did: `nameText()`-shaped control-char/
  angle-bracket rejection + NFC normalisation, wired into both
  `set-username` and `set-name` ahead of the `UPDATE` write) rather than
  re-litigated. `web/engine/src/**`: zero diff. No new `[[routes]]` block, no
  new binding, no new migration, no new endpoint — nothing for the
  new-endpoint checklist.
- **Frontend diff**: one file, `web/frontend/src/analysis/map-annotations.ts`
  (+3/−1) — mapbox-gl 3.30.0 decoupled pitch from rotation, so
  `disableMapInteractions()`'s interaction list gained `touchPitch`
  alongside the existing `dragRotate` entry, closing a gap where a
  two-finger pitch drag could tilt the map while an annotation was being
  drawn. Purely a UI-interaction-lock fix; no data flow from untrusted input,
  no DOM sink, no auth surface. Grepped the whole window's diff for
  `dangerouslySetInnerHTML`/`innerHTML`/`insertAdjacentHTML`/
  `document.write`: no hits.
- **Dependencies.** `bun audit` on `HEAD` (`06f7108`): **5 vulnerabilities (1
  critical, 3 moderate, 1 low), all `astro <7.0.6`** — identical severity
  breakdown to the post-fix count the 2026-09-09 round recorded, confirming
  **SEC-50 held**: none of `hono`/`js-yaml`/`sharp`/`svgo`/`smol-toml`
  reappeared. Cross-checked `package.json`'s `overrides` block against the
  2026-09-09 fix: `hono ^4.13.7`, `js-yaml ^4.3.2`, `sharp ^0.35.4`,
  `svgo ^4.1.0`, `smol-toml ^1.8.0` — all present, unreduced. The
  `upgrade-deps` cycle (#696) additionally caught and removed a **stray root
  `hono` dependency** that #695 (the previous *security* commit) had left in
  root `package.json`'s `dependencies` block rather than only the
  `overrides` block — the exact failure mode `docs/dependency-review-log.md`
  already warns about, this time introduced by a non-routine (security-fix)
  commit rather than a routine dependency-cycle one; #696's own write-up
  caught it via its "read root `dependencies` at the start of a run" check
  and folded the `hono`/`sharp` override floors into the real dependency
  specs instead (`^4.13.3`→`^4.13.7` and `^0.35.3`→`^0.35.4` respectively,
  in the actual `dependencies`/`peerDependencies` entries, not just the
  override). Confirmed on this round's `HEAD`: root `package.json` carries
  `hono` only inside `overrides`, not `dependencies` (`sed`-verified
  directly). No astro version bump this window (`^6.4.8` unchanged) — the
  5-advisory residual is the unmoved SEC-34/G-16 gap, not a new regression.
- **Verification runs**: `bun run typecheck:all` clean (all 4 workers +
  engine + frontend, unchanged from last round). `bun run test:all` →
  engine 1442 pass/2 skip/0 fail (77 files), frontend 787 pass (56 files),
  auth-api 108 pass/6 todo (9 files), competition-api 773 pass (45 files) —
  identical pass counts to the 2026-09-09 round, confirming no regression
  from either #695 landing or #696's dependency bumps.
- **Did not do**: the standing live-deploy gaps (CSRF PoC, cookie
  attributes, zone snapshot, CSP flip, email SPF/DKIM/DMARC) — no commit
  this window touches those surfaces. SEC-45 was again not attempted — still
  needs its own oracle-tested PR, now five rounds running. Did not attempt
  the `astro` 6→7 major upgrade (G-16): this window's only dependency
  changes were #696's routine minor/patch bumps (mapbox-gl, lucide-react,
  react-aria-components, react-router-dom, shadcn, tailwind-merge,
  `@internationalized/date`, `@hono/zod-validator`, `@playwright/test`,
  `@types/*`), none a major version and none touching `astro`; the major
  upgrade stays flagged for a dedicated `upgrade-deps` pass per the last two
  rounds' recommendation, not something to fold into a routine week's bump
  set or a security-review round.

### Executive summary

This was a thin review window — two commits, one of them the prior round's
own already-audited security PR and the other a routine dependency-upgrade
cycle whose only source change is a one-line mapbox-gl interaction-lock fix
with no security surface. **`bun audit` confirms SEC-50 held**: exactly 5
advisories, same severity mix as the post-fix count from 2026-09-09, all
`astro` (the pre-existing SEC-34/G-16 residual, unchanged composition — no
new CVE this week). The dependency cycle also cleaned up a small
process-hygiene issue on its own initiative: a stray `hono` entry in root
`package.json`'s `dependencies` (left there by the SEC-50 fix commit
instead of only the `overrides` block) was folded into the real dependency
specs across the frontend and worker `package.json` files, which is a
better fix than the override alone — it makes the patched floor visible to
anyone reading a workspace's own manifest rather than only the root
override. **No new findings.** SEC-45 remains the top open item, now five
rounds without a fix PR started; the `astro` 6→7 major upgrade (G-16)
remains unaddressed and is not something this round's dependency changes
touched. `typecheck:all` and `test:all` are green with pass counts
identical to last round across all four workspaces.

### New findings

None this round.

### Status changes

None this round — no prior finding's status moved. (SEC-50 and SEC-49 were
both closed in the 2026-09-09 round; this round re-verified both held, per
Methodology, without changing their register status.)

### Re-checked but no change (walked and clean this window)

- **SEC-50 (`bun audit`).** 5 advisories, all `astro`, identical severity
  breakdown to the 2026-09-09 post-fix count — confirmed held, no
  `hono`/`js-yaml`/`sharp`/`svgo`/`smol-toml` regression.
- **SEC-49 (`name-text.ts` / `set-name` / `set-username`).** Landed exactly
  as the 2026-09-09 round described; re-read, unchanged since.
- **SEC-10/SEC-11/SEC-12.** `auth-bypass.test.ts` present;
  `MAX_COMPRESSED_BYTES`/`MAX_DECOMPRESSED_BYTES`/`MAX_XCTSK_TURNPOINTS = 50`
  byte-identical in `igc-validation.ts`/`validators.ts`.
- **Engine algorithmic-DoS class (SEC-26/29/31/40/45).** Zero diff in
  `web/engine/src/**` this window — none of `route-optimizer.ts`,
  `task-optimizer.ts`, `turnpoint-sequence-path.ts`, the GPX/KML client
  parsers, or `thermal-shape.ts` changed.
- **Infra.** `wrangler.toml` ×5, `functions/**`, `**/migrations/**`,
  `.github/workflows/**`: zero diff across the whole window.
- **DOM sinks.** No `dangerouslySetInnerHTML`/`innerHTML`/
  `insertAdjacentHTML`/`document.write` introduced anywhere in the window's
  diff.
- **Root dependency hygiene.** `hono` confirmed present only in
  `overrides`, not `dependencies`, in root `package.json` on this round's
  `HEAD`.

### Scope gaps still not done

Unchanged from the [2026-09-09 round](2026-09-09.md)'s list — nothing in
this window's two commits touched any of these surfaces:

1. Dynamic CSRF PoC against the allowlisted CORS.
2. Cookie attribute verification on a live deploy.
3. Cloudflare zone settings snapshot (HSTS, TLS min, WAF, bot management).
4. Verify SEC-10 fix on a deployed comp-api endpoint.
5. Confirm no legacy `Cookie: test-user=…` acceptance in production — still
   only a source-level check.
6. TOCTOU / idempotency on `/api/user/tracks` + `/api/user/tasks` quota
   checks — still needs a live concurrency test.
7. **Flip CSP from Report-Only to enforced** — unchanged; still the same
   four inline-script blocks to nonce/hash first.
8. **SEC-26** packer/task-analysis decompression cap + test.
9. Extend `html-sinks.test.ts` from count-pinning towards content
   (statement-level `escapeHtml`/`esc` check) if a tenth SEC-41-class
   instance ever appears.
10. **SEC-45** — bound the PathFinder route-optimiser's branch-and-bound
    search; oracle tests + ~60k-fix adversarial regression, SEC-32/33
    style, in its own PR (rolls the engine generation → owes a
    `scoring-changes/` note + archive parity measurement). **Still the top
    open item — untouched a fifth round running.**
11. **SEC-40** — bound or restructure `findSubCores` in `thermal-shape.ts`.
12. **SEC-29** parser loop bounds + `file.size` pre-check + regression test.
13. **SEC-31** byte/fix-based task-analysis cap.
14. Give the engine's own `xctsk-parser.ts`/`route-optimizer.ts` a
    turnpoint-count cap independent of the API-layer Zod schema.
15. Email SPF/DKIM/DMARC on a live deploy.
16. **SEC-34 residual** — `astro` 6→7. Still carrying the Critical AVIF-RCE
    CVE (confirmed build-time-only/not exploitable); this window's
    dependency cycle bumped ten other packages but did not touch `astro`.
17. `encodeURIComponent` the ids in `TaskExportButtons.tsx:83-85`, and the
    same for `slugSegment()`'s id half in `lib/slug.ts` (Info-grade); decide
    whether `fetchWithRetry` should stop retrying non-404 4xx.
18. **`rateLimit` row expiry** (from SEC-39's fix) — rows never expire.
    Still open.
19. **G-20** — `bun install`/bare `bun update <pkg>` preserve an
    already-range-satisfying lockfile resolution rather than advancing to
    latest matching patch. Not re-triggered this round (no new CVE against
    an already-pinned version surfaced), but still an open process
    question for `upgrade-deps` to decide on.

### Where to start the next review

1. Commit reviewed up to: **HEAD = `06f7108`**. Both `.github/workflows/`
   and `functions/` were present in this session's checkout; if a future
   sandboxed session lacks `.github/workflows/`, use
   `diff <(git show <rev1>:path) <(git show <rev2>:path)` rather than a
   `git diff` pathspec, which silently returns empty there.
2. **SEC-45 is still the only open engine-DoS finding of its class and the
   top open item, now five rounds running without a fix PR.** It needs its
   own PR: oracle tests over the S7F 2026 suite, a bound on
   `computeBestProgress`'s `exactAt()` evaluations (or a time-based
   Lipschitz prune), a ~60k-fix adversarial wandering track under a hard
   timeout, a `scoring-changes/` note, and archive parity measurement.
3. **Verify SEC-50 still holds** — `bun audit` should read exactly 5
   advisories (the `astro` residual). If `hono`/`js-yaml`/`sharp`/`svgo`/
   `smol-toml` advisories reappear, check for a newer in-range patch release
   before assuming a code change caused it (the G-20 pattern).
4. **G-16 / astro 6→7** — still carrying a Critical CVE in its residual
   (confirmed build-time-only, not currently exploitable). Two rounds
   running now recommending this be prioritised with its own `upgrade-deps`
   pass; still not started.
5. Chase SEC-26/29/31/40 (gaps #8/#11/#12/#13) — none moved in five rounds
   now.
6. The CSP flip (gap #7) still needs the four-block inline-script inventory
   first.
7. Do NOT re-open SEC-03 (accepted by design).
