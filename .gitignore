# Playwright
.playwright-mcp/
playwright-report/
test-results/

# Dependencies
node_modules/

# Build outputs
dist/
.wrangler/
web/frontend/public/js/

# The scoring engine's generation, derived from its own sources by
# web/scripts/generate-scoring-fingerprint.ts. Deliberately not committed:
# a checked-in hash is a merge conflict on every pair of parallel engine
# branches, and one that no side of the merge holds the right answer to.
# See docs/scoring-version.md.
web/engine/src/scoring-fingerprint.generated.ts

# Environment files
.env
.env.local
.dev.vars

# IDE
.idea/
*.swp
*.swo
.vscode/

# Claude Code
.claude/worktrees/
.claude/agent-memory/

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# TypeScript cache
*.tsbuildinfo
.codex/environments/environment.toml

# SSR bundle build artifact (web/frontend/dist-ssr)
dist-ssr/
