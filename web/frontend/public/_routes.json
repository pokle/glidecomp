{
  "version": 1,
  "description": "Send /api/* to Pages Functions; everything else is a static asset. Checked in explicitly because Cloudflare's auto-generated routes manifest has stranded new prefixes (/api/user/, /api/u/) at 404 on preview deployments — see the user-files migration PR.",
  "include": ["/api/*"],
  "exclude": []
}
