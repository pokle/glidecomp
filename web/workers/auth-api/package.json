{
  "name": "auth-api",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "wrangler d1 execute taskscore-auth --local --persist-to ../../.wrangler/state --file=src/db/schema.sql && wrangler dev --persist-to ../../.wrangler/state",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "better-auth": "^1.5.6",
    "hono": "^4.12.10",
    "kysely": "^0.28.15",
    "kysely-d1": "^0.4.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260404.1",
    "typescript": "^6.0.2",
    "wrangler": "^4.80.0"
  }
}
