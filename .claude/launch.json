{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "frontend",
      "runtimeExecutable": "/Users/tushar/.bun/bin/bun",
      "runtimeArgs": ["run", "dev:frontend"],
      "port": 3000
    },
    {
      // auth-api, competition-api and airscore-api all run in this one
      // wrangler session (see web/scripts/dev-workers.sh) and are reached
      // through the dev-router on 8790. There is no per-worker port to attach
      // to any more; the session's inspector covers all of them.
      "name": "workers",
      "runtimeExecutable": "/Users/tushar/.bun/bin/bun",
      "runtimeArgs": ["run", "dev:workers"],
      "port": 8790
    }
  ]
}
